package com.harekrishna.ui.theme

import androidx.compose.ui.graphics.Color

// Premium devotional palette — see android/DESIGN.md for the full rationale.
// Dark-first. Warm deep base. Saffron primary, soft devotional purple
// secondary, festive gold for rare celebratory moments.

val InkBlack      = Color(0xFF0E0B14)   // app background
val Surface       = Color(0xFF19131F)   // raised surface (cards, sheets)
val SurfaceRaised = Color(0xFF241C2D)   // hover / pressed / focus

val Saffron       = Color(0xFFFFA62B)   // primary — actions, focus, the counter
val SaffronDeep   = Color(0xFF1A0F00)   // text/icons on Saffron
val DevoPurple    = Color(0xFF8B5CF6)   // secondary — accents, info chips
val FestiveGold   = Color(0xFFFFD700)   // tertiary — winners, finale moments

val WarmWhite     = Color(0xFFF5F1E8)   // primary text on dark
val MutedSand     = Color(0xFFA19A8E)   // secondary/muted text on dark
val ErrorRed      = Color(0xFFEF4444)
